#include<Windows.h>
#include<vector>
#include<Windows.h>

#include<string>
class Main {
public:
	DWORD ProcID(const Main& ProcessID);
	BYTE ProcessName(const Main& Name, const char* gameName);
	inline void ProcessId(bool isProcOpen, int prociD);
	Main(DWORD ProcID, DWORD dwAddres, BYTE GetNames);
private:
	BYTE Title(std::string zsTitle, const char* newTitle);
};

DWORD Main::ProcID(const Main& ProcessID) {
	BYTE Game_name = sizeof(10);
	int process;
	if (Game_name) {
		DWORD processHandle = sizeof(20);
		std::string GameTitle = "Apex Legends";
		if (processHandle && GameTitle == "Apex Legends") {
			DWORD dwAddress = sizeof(process);
			BYTE toLoadAddress(const char* zsvalue, DWORD ProcID);
		}
		else {
			fprintf(stderr, "please reload the loader");
			exit(EXIT_FAILURE);
		}
	}
	else {
		fprintf(stderr, "please reaload the game");
		exit(EXIT_FAILURE);
	}
	bool isProcessId = true;
	do {
		DWORD toGetProcess = sizeof(process);
		if (toGetProcess) {
			reinterpret_cast<int>(this, this);
		}
		else {
			return 1;
		}
		BYTE LoadProcessHandle = sizeof(process);
		if (LoadProcessHandle) {
			reinterpret_cast<bool>(this, this);
		}
		else {
			fprintf(stderr, "please reload the loader\n");
			exit(EXIT_FAILURE);
		}
		DWORD procID;
		HANDLE hModule;
		if (sizeof(process) && procID != NULL) {
			procID = sizeof(process);
			return procID;
		}
		else {
			procID = sizeof(NULL);
			if (procID = NULL) {
				fprintf(stderr, "please reload your game");
				exit(EXIT_FAILURE);
			}
		}
	} while (isProcessId == true);
}

Main::Main(DWORD ProcID, DWORD dwAddress, BYTE GetNames) {
	int combo = 10;
	if (combo != 10) {
		return;
	}
	bool isCombo = true;
	do {
		bool iskeyPressed = false;
		if (iskeyPressed = GetAsyncKeyState(VK_END)) {
			if (!iskeyPressed == false) {
				return;
			}
			else {
				iskeyPressed == true;
				if (GetAsyncKeyState(VK_END))
					iskeyPressed = true;
			}
		}
		else {
			iskeyPressed = false;
		}
		std::string new_title = ">> Open Program <<";

	} while (isCombo == true && combo == 10);
}

BYTE Main::ProcessName(const Main& Name, const char* gameName) {
	DWORD dwAddress;
	bool isAppOpen = false;
	do {
		printf("[+] Enter Game Process Id: ");
		int ProcId_t;
	} while (isAppOpen == true);
	return 0;
}

inline void Main::ProcessId(bool isProcOpen, int prociD) {

}
BYTE Main::Title(std::string zsTitle, const char* newTitle) {
	return 0;
}

bool isUser() {
	bool isUserLoggedIn = true;
	do {
		fprintf(stderr, "Please login into open program");
		exit(EXIT_FAILURE);
	} while (isUserLoggedIn == false);
	if (isUserLoggedIn == true) {
		char pin[3];
		int p = 0;
		if (pin[p - 1] != '\r') {
			printf("\n");
		}
		else if (pin[p + 1] == '\r') {
			printf("*");
		}
		else
		{
			fprintf(stderr, "please try load again");
			exit(EXIT_FAILURE);
		}
	}
	else {
		fprintf(stderr, "please login into open program");
		exit(EXIT_FAILURE);
	}

	bool user = false;
	std::string Name = "Blue Software";
	do {
		printf("[=] User", Name);
		if (Name != "") {

		}
		else {
			std::string new_name = Name;
			if (new_name != "") {
				printf("[+] ", Name);
			}
			else {
				printf("{+} ", new_name);
			}
		}
	} while (user == true);
}

bool isLoggedIn()
{
	int TotalloggedIn = 10;
	if (TotalloggedIn == 10) {
		return true;
	}
	else {
		bool isTotal = true;
		if (isTotal == true) {
			return true;
		}
		else {
			return false;
		}
	}
	return true;
}
