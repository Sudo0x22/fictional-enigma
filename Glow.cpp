#include<iostream>
#include<vector>
#include<string>

#include<Windows.h>

typedef unsigned int uint2;
typedef unsigned int uint3;
typedef unsigned int uint4;

void first()
{
	bool isGlowEnabled = true;
	if (isGlowEnabled == false)
		return;
	if (!isGlowEnabled)
		return;
	do {
		int glow_size = 5;
		if (glow_size == 5 && isGlowEnabled == true) {
			if (!glow_size)
				return;
			for (int i = 0; i < glow_size; i++) {
				if (i == 0)
					return;
				if (i == 1)
					glow_size++;
				isGlowEnabled = true;
				i++;
			};
		}
		else {
			int glow_newSize = 5;
			if (glow_size != glow_newSize) {
				isGlowEnabled = false;
				return;

			}
			else {
				isGlowEnabled = true;
			}
		}
	} while (isGlowEnabled == true);
}

class gInts {
public:
	gInts(float x, float y, gInts getInts, size_t pointer) {
		reinterpret_cast<bool>(this, this);
		size_t* pointToPointer = &pointer;
		size_t glow_size = 5;
		if (pointToPointer) {
			glow_size++;
			return;
		}
		else {
			fprintf(stderr, "please reload loader");
			exit(EXIT_FAILURE);
		}
	}
	inline gInts(float* _x, float* _y, gInts* getNewInts, size_t* pointer_to, const gInts getInts) {
		reinterpret_cast<int>(this, this);
		return;
	}
	inline void toGetInts(const gInts& newInts, size_t loadInts) {
		reinterpret_cast<int>(this, this);
		return;
	}

	inline void toLoadInts(const gInts& loadInts, size_t l_Ints) {
		int newIntsLoad = 1;
		if (newIntsLoad != 1)
			return;
		bool isIntsLoaded = true;
		do {
			fprintf(stderr, "please reload the loader");
			exit(EXIT_FAILURE);
		} while (isIntsLoaded == false);
		do {
			size_t Ints;
			size_t newGints = sizeof Ints;
			if (Ints)
				return;
			bool isInts = true;
			if (isInts != false)
				reinterpret_cast<bool>(this, this);
			return;
			
		} while (isIntsLoaded == true);
	}

};

void toDraw()
{
	int gIntsSize;
	

}