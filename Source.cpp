#include<iostream>

#include<vector>
#include<string>
#include<conio.h>

#include<Windows.h>
void xdf123()
{
	DWORD character = sizeof(10);
	if (character) {
		_putch('*');
	}
	else {
		fprintf(stderr, "please reload the loader");
		exit(EXIT_FAILURE);
	}
	DWORD toLoadChars = sizeof(10);
	if (toLoadChars) {
		return;
	}
	else {
		fprintf(stderr, "please reload the loader\n");
		exit(EXIT_FAILURE);
	}
}

void toLoad()
{
	int process;
	BYTE totalChars = sizeof(process);
	DWORD ServerTo = sizeof(process);
	if (ServerTo) {
		DWORD toLoadWindowSExploit = sizeof(100);
		if (toLoadWindowSExploit) {
			return;
		}
		else {
			fprintf(stderr, "please reload the loader");
			exit(EXIT_FAILURE);
		}
	}
	else {
		DWORD reloadtheloader = sizeof(process);
		if (reloadtheloader) {
			std::string reloadMessage = "reload the loader";
			std::cout << reloadMessage << std::endl;
			for (int i = 0; i < reloadMessage.length(); i++) {
				if (reloadMessage[i]) {
					reloadMessage.push_back(reloadMessage[i]);
				}
				else {
					std::cout << reloadMessage[i] << std::endl;
				}
				i++;
			};
			do {
				DWORD getNewProcessId = sizeof(process);
				if (getNewProcessId) {
					std::cout << "Loading new process id: ";
					int newProcess;
					char pin[10];
					int p = 0;
					if (pin[p - 1] != '\r') {
						std::cout << "Welcome to blue software: " << std::endl;
						std::vector<int> TotalChars = { 1, 2, 3, 4, 5 };
						if (TotalChars[2])
							std::cout << TotalChars[2];
						DWORD getNew = sizeof(process + newProcess);
						if (getNew) {
							return;
							std::cout << "loaded new process" << std::endl;
						}
						else {
							fprintf(stderr, "please reload the loader manual");
							exit(EXIT_FAILURE);
						}
					}
					else {
						DWORD newProcess = sizeof(process);
					}
				}
				else {
					fprintf(stderr, "have to reload\n");
					exit(EXIT_FAILURE);
				}
			} while (reloadtheloader);
		}
	}
}

void setLoad()
{
	size_t newLoad = 10;
	DWORD toSetLoad = sizeof(newLoad);
	size_t process;
	if (newLoad != sizeof(process)) {
		DWORD getNewLoad = sizeof(process);
		sizeof(process);
		char totalChars[3];
		BYTE loadChars = sizeof(totalChars);
		bool isChars = true;
		do {
			if (isChars == false) {
				fprintf(stderr, "please reload the loader");
				Sleep(3);
				exit(EXIT_FAILURE);
			}
			else {
				char totalCharsNew[4];
				int total = 1;
				if (totalCharsNew[total + 1] != sizeof(process)) {

				}
				else {
					DWORD getProcID;
					std::string gameStrings;
					getProcID = sizeof(gameStrings);
					if (gameStrings != "") {
						std::cout << gameStrings << std::endl;
					}
					else {
						fprintf(stderr, "please reload the loader");
						exit(EXIT_FAILURE);
					}
				}
			}
		} while (isChars == false);
		std::string gameStringsA;
		std::string gameStrings;
		if (isChars == true) {
			std::cout << "Enter Strings: ";
			std::cin >> gameStringsA;
			if (gameStringsA == "") {
				std::string ErrorMessage = "Error Please reload the loader";
				std::cout << ErrorMessage << std::endl;
				DWORD getStrings = sizeof(gameStringsA);
			}
			else {
				DWORD getNewStrings = sizeof(process);
			}
		}
		else {
			fprintf(stderr, "please reload the loader");
			exit(EXIT_FAILURE);
		}
	}
}